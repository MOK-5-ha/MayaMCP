# Google GenAI SDK (new unified SDK, replaces google-generativeai)
google-genai>=2.23.0


# Core dependencies
python-dotenv>=1.0.0
requests>=2.32.2,<3.0.0
websockets>=15.0,<17.0
tenacity>=9.0.0
# Google Cloud Trace, OpenTelemetry & Vertex AI Gen AI Evaluation
google-cloud-aiplatform>=1.70.0
google-cloud-trace>=1.11.0
opentelemetry-api>=1.20.0,<=1.42.1
opentelemetry-sdk>=1.20.0,<=1.42.1
opentelemetry-exporter-gcp-trace>=1.7.0
pandas>=2.0.0
scikit-learn>=1.9.1
rouge-score>=0.1.2

# UI (Gradio 6.x - upgraded from 5.x to patch CVEs PYSEC-2026-63/64/65/66/2178/2179)
gradio>=6.27.0,<7.0.0

# TTS (stay on Cartesia 2.x for stability)
cartesia>=2.0.0,<3.0.0

# Visualization
pillow>=12.3.0,<13.0

# Testing dependencies
hypothesis>=6.168.0
pytest>=9.1.1,<10.0.0
pytest-bdd>=8.1.0

# Google Agent Development Kit (ADK)
google-adk>=2.9.0
fastapi>=0.141.1,<1.0
a2a-sdk>=0.3.22,<0.4.0
sse-starlette>=3.4.11
starlette>=1.6.0
aiohttp>=3.14.3


# RAG dependencies
# Memvid dependencies (optional - will fallback if not available)
qrcode[pil]
opencv-python>=4.11.0

# Numpy is required by Memvid
numpy>=2.0.0,<2.3.0

# llm-guard is commented out because it has torch requirements incompatible with Python 3.13.
# The codebase falls back gracefully to regex scanning when llm-guard is unavailable.
# llm-guard>=0.3.0
cryptography>=50.0.1

# Coinbase Developer Platform (CDP) SDK
cdp-sdk>=1.48.1